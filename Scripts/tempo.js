function TimeControl() {
    let ct = document.getElementsByClassName("ytp-hide-info-bar");

    if (ct.currentTime = 1.41.11) {
        ct.classList.remove("playing-mode");
        ct.classList.add("playing-mode")
    }
};
